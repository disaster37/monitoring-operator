
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.7.0
  creationTimestamp: null
  name: centreons.monitor.k8s.webcenter.fr
spec:
  group: monitor.k8s.webcenter.fr
  names:
    kind: Centreon
    listKind: CentreonList
    plural: centreons
    singular: centreon
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Centreon is the Schema for the centreons API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: CentreonSpec defines the desired state of Centreon
            properties:
              endpoint:
                description: The endpoint default setting
                properties:
                  activeService:
                    description: By default, activate service when created it Default
                      to true
                    type: boolean
                  arguments:
                    description: The default command arguements to set when create
                      service You can use special tag to generate value on the flow
                    items:
                      type: string
                    type: array
                  categories:
                    description: Default categories
                    items:
                      type: string
                    type: array
                  defaultHost:
                    description: The default host to attach service
                    type: string
                  macros:
                    additionalProperties:
                      type: string
                    description: The default macro to set when create service You
                      can use special tag to generate value on the flow
                    type: object
                  nameTemplate:
                    description: The default template name when create service from
                      endpoint
                    type: string
                  serviceGroups:
                    description: Default service groups
                    items:
                      type: string
                    type: array
                  template:
                    description: The default service template to use when create service
                      from endpoint It normally optional, but Centreon bug impose
                      to set an existed template
                    type: string
                required:
                - template
                type: object
            required:
            - endpoint
            type: object
          status:
            description: CentreonStatus defines the observed state of Centreon
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
