apiVersion: monitor.k8s.webcenter.fr/v1
kind: Platform
metadata:
  name: centreon
spec:
  isDefault: true
  type: centreon
  debug: true
  centreonSettings:
    url: "http://centreon-test.default.svc/centreon/api/index.php"
    selfSignedCertificat: true
    secret: centreon
